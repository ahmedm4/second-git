# 📊 مجلد البيانات

## ملف المثال: sounds-example.json

هذا ملف مثال يوضح البنية الصحيحة لملف JSON الذي يحتوي على الأصوات.

### 🔧 كيفية الاستخدام

#### 1. استخدام ملف محلي

إذا أردت استخدام ملف محلي بدلاً من API خارجي:

**خطوة 1:** انسخ `sounds-example.json` إلى `sounds.json`
```bash
cp data/sounds-example.json data/sounds.json
```

**خطوة 2:** عدّل في `js/data.js`:
```javascript
const SOUNDS_JSON_URL = './data/sounds.json';
```

**خطوة 3:** شغّل التطبيق من خادم محلي (مهم!)
```bash
python -m http.server 8000
```

**ملاحظة:** لن يعمل الملف المحلي إذا فتحت `index.html` مباشرة بسبب قيود CORS. يجب استخدام خادم محلي.

#### 2. تخصيص البيانات

افتح `sounds.json` وعدّل/أضف البيانات:

**لإضافة دعاء:**
```json
{
  "dua_name_key": {
    "name": "اسم الدعاء",
    "synonyms": ["مرادف1", "مرادف2"],
    "sounds": [
      {
        "url": "رابط الملف الصوتي",
        "reader": "اسم القارئ",
        "readerSynonyms": ["مرادف"],
        "image": "رابط الصورة"
      }
    ]
  }
}
```

**لإضافة لطمية:**
أضف في قسم `latmiyat.sounds`:
```json
{
  "title": "عنوان اللطمية",
  "titleSynonyms": ["مرادف1", "مرادف2"],
  "url": "رابط الملف الصوتي",
  "reader": "اسم الرادود",
  "readerSynonyms": ["مرادف"],
  "image": "رابط الصورة"
}
```

### 📝 نصائح

1. **استخدم أدوات التحقق من JSON:**
   - [JSONLint](https://jsonlint.com/) - للتحقق من صحة البنية
   - [JSON Formatter](https://jsonformatter.org/) - لتنسيق الملف

2. **المرادفات مهمة:**
   - أضف جميع الأشكال الممكنة للكلمة
   - مثال: "كميل", "كوميل", "كُميل", "دعاء كميل"

3. **الصور:**
   - يمكن استخدام روابط خارجية
   - أو وضع الصور في `images/` واستخدام روابط نسبية
   - مثال: `"image": "./images/kumail.jpg"`

4. **الأصوات:**
   - استخدم روابط HTTPS
   - تأكد من أن الملفات متاحة ويمكن الوصول إليها
   - صيغة MP3 موصى بها

### ⚠️ ملاحظات مهمة

- **لا تحذف قسم `latmiyat`** - التطبيق يعتمد عليه
- **احفظ نسخة احتياطية** قبل التعديل
- **جرّب الملف** بعد كل تعديل للتأكد من صحته

### 🔍 التصحيح

إذا لم تظهر البيانات:

1. افتح Console في المتصفح (F12)
2. ابحث عن أخطاء JSON
3. تأكد من صحة الروابط
4. تأكد من استخدام خادم محلي

### 📖 للمزيد

راجع [API_DOCUMENTATION.md](../API_DOCUMENTATION.md) للتفاصيل الكاملة.
